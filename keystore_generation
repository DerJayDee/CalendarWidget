In a dir containing keytool (usually jdk dir, e.g.: C:\Program Files\Java\jdk1.8.0_71\bin)
replace <KEYSTORE NAME> and <KEY NAME> with appropriate values.

keytool -genkey -v -keystore <KEYSTORE NAME> -alias <KEY NAME> -keyalg RSA -keysize 2048 -validity 10000